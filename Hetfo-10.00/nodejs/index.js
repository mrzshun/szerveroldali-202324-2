const { myadd } = require('./user.js');
//console.log(`${user.getName()} lives in ${user.getLocation()} and was born on ${user.dob}.`);

let a = 2;
let b = 3;
console.log('%d plus %d equals to %d',a,b,myadd(a,b));
